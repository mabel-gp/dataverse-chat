export const renderStatistics = () => {
 
  const statistics = document.createElement("section");
  statistics.className = "prueba"
  
  statistics.innerHTML = 

      `
        <div id="estadistica"></div>
      `
      
  return statistics;
      
};